{:flg; ... :}
=============

Set a flag for a bif.

```html
{:flg; flag1 flag2 ... :}
```

It must be included in the parameters of a bif, by itself it has no utility.

Example:

```html
{:include; {:flg; require :} >> ... :}
```
